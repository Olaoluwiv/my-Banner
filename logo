<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Betzycoded Logo</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .logo-container {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 200px;
            height: 200px;
            background: linear-gradient(135deg, #007bff, #ff5722, #00c853);
            border-radius: 50%;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 50px auto;
            position: relative;
        }
        .logo-container img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 50%;
            border: 3px solid #fff;
        }
        .logo-container svg {
            position: absolute;
            width: 100%;
            height: 100%;
            animation: rotate 10s linear infinite;
        }
        .logo-container text {
            font-family: 'Arial', sans-serif;
            font-size: 16px;
            fill: #fff;
            font-weight: bold;
        }
        @keyframes rotate {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <div class="logo-container">
        <img src="https://avatars.githubusercontent.com/u/132825905?v=4" alt="Olaoluwa Ayidina">
        <svg viewBox="0 0 200 200">
            <path id="curve" d="M50,100 a1,1 0 0,1 100,0" fill="transparent"/>
            <text width="500">
                <textPath href="#curve" startOffset="50%">
                    Betzycoded
                </textPath>
            </text>
        </svg>
    </div>
</body>
</html>
